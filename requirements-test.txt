# Core test framework
pytest>=7.0
pytest-cov>=4.0          # optional – code‑coverage reporting
requests>=2.28            # for HTTP calls in integration tests

# Mocking & async helpers
pytest-mock>=3.10
fakeredis>=2.0            # if you ever need a Redis mock

# Database driver (used only by integration tests)
psycopg2-binary>=2.9

# Optional: for loading .env files in tests
python-dotenv>=1.0
