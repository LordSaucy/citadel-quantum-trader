name: Verify Checksums

on:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Re‑calculate hashes
        run: |
          sha256sum -c checksums.txt || { echo "Checksum mismatch!"; exit 1; }
