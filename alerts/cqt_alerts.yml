groups:
  - name: citadel-quantum-trader
    rules:

    # 1️⃣ Draw‑down > 5 % (critical – kill‑switch will fire)
    - alert: DrawdownCritical
      expr: drawdown_pct > 0.05
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "Draw‑down > 5 % on bucket {{ $labels.bucket_id }}"
        description: |
          Current draw‑down: {{ printf "%.2f" $value }}%
          The kill‑switch will pause new entries at 15 % draw‑down.
          Investigate immediately.

    # 2️⃣ Latency > 150 ms (warning)
    - alert: LatencySpike
      expr: histogram_quantile(0.95, rate(order_latency_seconds_bucket[5m])) > 0.15
      for: 2m
      labels:
        severity: warning
      annotations:
        summary: "Latency > 150 ms (95th percentile)"
        description: |
          Average 95th‑percentile latency over the last 5 min: {{ printf "%.3f" $value }} s.
          May indicate network congestion or broker overload.

    # 3️⃣ Order‑rejection surge (>2 % of orders)
    - alert: OrderRejectionSurge
      expr: (increase(order_reject_total[5m]) / increase(order_total[5m])) > 0.02
      for: 3m
      labels:
        severity: warning
      annotations:
        summary: "Order rejection rate > 2 %"
        description: |
          Rejection rate: {{ printf "%.2f" $value }}%
          Possible causes: insufficient margin, market closed, bad price.

    # 4️⃣ Global risk‑budget approaching limit
    - alert: GlobalRiskHigh
      expr: global_risk_percentage > 0.04   # 80 % of the 5 % limit
      for: 1m
      labels:
        severity: warning
      annotations:
        summary: "Global risk budget > 80 % of limit"
        description: |
          Current utilization: {{ printf "%.2f" $value }}%
          The controller will start throttling risk fractions.

groups:
  - name: critical-alerts
    interval: 1m
    rules:

    # -------------------------------------------------
    # 1️⃣ Latency‑spike – avg latency > 0.25 s over 1 min
    # -------------------------------------------------
    - alert: HighOrderLatency
      expr: avg_over_time(rate(order_latency_seconds_sum[1m]) / rate(order_latency_seconds_count[1m])[1m]) > 0.25
      for: 2m
      labels:
        severity: critical
      annotations:
        summary: "⚡️ Order latency > 250 ms (average over 1 min)"
        description: |
          The bot is experiencing high round‑trip latency.
          Current avg latency: {{ $value | printf "%.3f" }} s.
          Check network path to the MT5 broker, VPN health, or broker API throttling.

    # -------------------------------------------------
    # 2️⃣ Order‑rejection‑burst – > 2 % rejections over 5 min
    # -------------------------------------------------
    - alert: OrderRejectionBurst
      expr: (rate(order_reject_total[5m]) / rate(order_total[5m])) > 0.02
      for: 3m
      labels:
        severity: warning
      annotations:
        summary: "🚫 Order‑rejection rate > 2 % (5‑min window)"
        description: |
          The bot is being rejected by the broker too often.
          Rejection rate: {{ $value | printf \"%.2%\" }}.
          Investigate broker limits, depth checks, or slippage guards.

          groups:
  - name: critical-alerts
    rules:
      - alert: GlobalRiskExceeded
        expr: global_risk_percentage > 4.5
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Global risk budget > 4.5 %"
          description: "Aggregated risk exposure is approaching the 5 % cap. The controller will auto‑scale down soon."
          runbook: "https://github.com/yourorg/citadel-qt/blob/main/docs/runbook.md#global-risk-budget"

      - alert: EdgeDecayTriggered
        expr: increase(edge_decay_events_total[5m]) > 0
        for: 0m
        labels:
          severity: warning
        annotations:
          summary: "Edge‑decay detector fired"
          description: "Rolling win‑rate fell below the safety floor; risk fractions have been halved for affected buckets."
          runbook: "https://github.com/yourorg/citadel-qt/blob/main/docs/runbook.md#edge-decay"

          groups:
  - name: streak-alerts
    interval: 1m
    rules:
      - alert: LongLossStreak
        expr: max(bucket_streak_losses) by (bucket_id) > 3
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Bucket {{ $labels.bucket_id }} has a {{ $value }}‑trade loss streak"
          description: "The bot has lost {{ $value }} trades in a row on bucket {{ $labels.bucket_id }}. Consider pausing or investigating the market regime."




